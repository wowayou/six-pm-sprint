{
  "name": "six-pm-sprint",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "《六点夺秒》——45 秒、单指即玩的下班生存挑战。",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test",
    "audit:fairness": "node scripts/audit-fairness.mjs",
    "check": "node --check src/engine.js && node --check src/game.js && node --check sw.js && node --check scripts/dev-server.mjs && node --check scripts/audit-fairness.mjs && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
