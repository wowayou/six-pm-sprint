{
  "name": "six-pm-sprint",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "《六点夺秒》——45 秒、单指即玩的下班生存挑战。",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test",
    "check": "node --check src/engine.js && node --check src/game.js && node --check scripts/dev-server.mjs && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
